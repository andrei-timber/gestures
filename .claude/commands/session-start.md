Run the session-start ritual before making any changes.

Read `docs/STATUS.md`, then orient in 2-3 sentences: where work stands, what's next, any blockers.
Then propose and agree **Scope + Definition-of-done** for this session before editing anything. This is
a plan-gate whether or not plan mode is active — apply nothing until the scope is agreed.

Canonical steps: `CLAUDE.md` → Work cadence.
