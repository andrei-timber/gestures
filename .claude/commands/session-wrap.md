Run the session-wrap ritual to hand the session over cleanly. This is a lightweight reconcile, not a
milestone close.

List the intended wrap actions and confirm before acting. Then: tick done steps and reset the "Now"
block in `docs/STATUS.md` (next step + its verify command); append any decisions to `docs/decisions.md`
(Y-statement shape); park discovered out-of-scope work in the STATUS Follow-ups queue; suggest a
semantically-contained commit grouping. **Never auto-commit.**

Canonical steps: `CLAUDE.md` → Work cadence.
