# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env / API keys — Vite inlines VITE_* at build; never commit real keys (.env.example is the template)
.env
.env.*
!.env.example

# Wrangler (Cloudflare) local state / cache
.wrangler

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright MCP browser-verify artifacts (screenshots, snapshots)
.playwright-mcp/
